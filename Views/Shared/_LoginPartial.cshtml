{

@if (User.Identity.IsAuthenticated) {
<div class="flex-row-reverse collapse navbar-collapse">

    <ul class="navbar-nav ">


        <li class="nav-item">
            @Html.ActionLink("Hello " + User.Identity.Name + "!", "Edit", "User", new { id = User.Identity.Name }, htmlAttributes: new { title = "Edit Profile", @class = "nav-link active" })
        </li>
        <li class="nav-item">
            @Html.ActionLink("Current Jobs", "CurrentJobs", "Freelancer", new { id = User.Identity.Name }, htmlAttributes: new { title = "Current Jobs", @class = "nav-link active" })
        </li>
        <li class="nav-item">
            @Html.ActionLink("Saved Posts", "Saved", "Freelancer", routeValues: new { id = User.Identity.Name }, htmlAttributes: new { title = "Saved Posts", @class = "nav-link active" })
        </li>
        <li class="nav-item">
            @Html.ActionLink("Log Out", "LogOut", "User", new { }, htmlAttributes: new { title = "Log Out", @class = "nav-link active" })
        </li>
    </ul>
</div>
}
else
{
<div class=" flex-row-reverse collapse navbar-collapse">
    <ul class="navbar-nav ">
        <li class="nav-item">
            <!-- Button trigger LoginModal -->

            <button type="button" class="nav-link active btn btn-link" data-bs-toggle="modal" data-bs-target="#LoginModal">
                Login
            </button>
        </li>
        <li class="nav-item">
            <!-- Button trigger LoginModal -->
            <button type="button" class="nav-link active btn btn-link" data-bs-toggle="modal" data-bs-target="#RegisterModal">
                Register
            </button>
        </li>
    </ul>
</div>
}







